<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="80dp">

        <android.support.v7.widget.CardView
            android:id="@+id/marquee_card"
            android:layout_width="80dp"
            android:layout_height="80dp"
            app:cardUseCompatPadding="true"
            app:cardCornerRadius="5dp"
            app:cardPreventCornerOverlap="true"
            app:cardElevation="5dp">

                <ImageView
                    android:id="@+id/img_marquee_item"
                    android:layout_width="70dp"
                    android:layout_height="70dp"
                    android:scaleType="centerCrop"
                    android:src="@drawable/a1"/>

        </android.support.v7.widget.CardView>

        <TextView
            android:id="@+id/tv_marquee_title_one"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:paddingTop="10dp"
            android:textColor="@android:color/black"
            android:layout_toRightOf="@+id/marquee_card"
            android:layout_alignTop="@id/marquee_card"/>

        <TextView
            android:id="@+id/tv_marquee_title_two"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:textColor="@android:color/black"
            android:layout_marginLeft="10dp"
            android:layout_marginRight="10dp"
            android:layout_toRightOf="@+id/marquee_card"
            android:layout_alignBottom="@id/marquee_card"/>

</RelativeLayout>